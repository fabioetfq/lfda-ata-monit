<h1>Pedidos</h1>

<div>
  <ul>
    <% @carts.each do|i| %>
      <li><%= i.id %>: <%= i.item.name %> - <%= i.qty_item %> | Alterar quantidade | <%= link_to 'Excluir solicitação', cart_path(i), data: {turbo_method: :delete, turbo_confirm: 'Confirma?'} %> </li>
    <% end %>
  </ul>
</div>